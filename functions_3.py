import math

from functions_2 import volume_cone
import functions
from math import pow, floor, ceil
print(pow(2, 3))
print(floor(pow(9.999)))
print(volume_cone(10, 20, decimal=3))
math.sqrt()

functions.area(4)
functions.sayHi("Teka")
